export enum Providers {
  Email = "email",
  Google = "google",
  Github = "github",
  Discord = "discord",
}
export enum PriorityEnum {
  High = 'high',
  Medium = 'medium',
  Low = 'low',
}
export enum StatusEnum {
  Todo = 'todo',
  In_Progress = 'in_progress',
  Done = 'done',
}